ssh-prod() {
  local production_ip
  production_ip=( $(ssh production-deploy 'cd ~/convertkit-deploy; ansible all --list | awk "NR > 2 { print }" | shuf -n 1 | tr -d " "') )

  ssh -t $production_ip "sudo su -c 'cd /srv/ckapp/current && bundle exec rails c' -s /bin/sh ckapp"
}
